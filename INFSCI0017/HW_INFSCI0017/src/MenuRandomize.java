/**
 * Class MenuRandomize
 * @author yec24
 * created: 11/1/2019
 */

// Assignment 2 below, not applicable to assignment 3 or latter

/*
import java.util.ArrayList;
import java.util.Random;

public class MenuRandomize {
	private ArrayList<Entree> entrees;
	private ArrayList<Side> sides;
	private ArrayList<Salad> salads;
	private ArrayList<Dessert> desserts;
	
	/*
	 * Constructor
	 * @param entreeFile a String of entrees filename
	 * @param sideFile a String of sides filename
	 * @param saladFile a String of salads filename
	 * @param dessertFile a String of desserts filename
	 *
	public MenuRandomize(String entreeFile, String sideFile, String saladFile, String dessertFile) {
		this.entrees = FileManager.readEntrees(entreeFile);
		this.sides = FileManager.readSides(sideFile);
		this.salads = FileManager.readSalads(saladFile);
		this.desserts = FileManager.readDesserts(dessertFile);
	}
	
	/**
	 * Method for random pick menu from ArrayLists
	 * @return a Menu of randMenu
	 *
	public Menu randomMenu() {
		Random rand = new Random();
		Entree randEntree = entrees.get(rand.nextInt(entrees.size()));
		Side randSide = sides.get(rand.nextInt(sides.size()));
		Salad randSalad = salads.get(rand.nextInt(salads.size()));
		Dessert randDessert = desserts.get(rand.nextInt(desserts.size()));
		Menu randMenu = new Menu("Chef Special", randEntree, randSide, randSalad, randDessert);
		return randMenu;
	}

}
*/
